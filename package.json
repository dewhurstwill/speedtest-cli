{
  "name": "speedtest-service",
  "version": "1.0.4",
  "description": "A simple speedtest CLI and package that leverages the Speedtest.net CLI tool.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/dewhurstwill/speedtest-service.git"
  },
  "scripts": {
    "install-cli": "npm install && npm link && speedtest-cli help",
    "uninstall-cli": "npm unlink speedtest-cli",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Will Dewhurst",
  "license": "MIT",
  "dependencies": {
    "is-online": "^9.0.1"
  },
  "bin": {
    "speedtest-cli": "./bin/cli.js"
  }
}
